MoraLinkGOst is a lightweight Windows service agent written in Go.

It runs at system startup, before user login, and performs background
synchronization tasks without exposing any user interface.

MoraLinkGOst is designed to operate quietly and continuously, acting as the
system-level counterpart to the MoraLink/SharkBusiness ecosystem.